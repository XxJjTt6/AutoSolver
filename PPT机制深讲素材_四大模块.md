# PPT 机制深讲素材：系统总览 / Agent Harness / Loop Engineering / 长期记忆

> 生成方式：9 路子代理全量深读代码 + 101 条关键口径逐条回查核验（98 条完全确认，3 条为行号/页码级修正）。
> 所有数字均带代码出处（文件:行号），可直接应对评委追问。
> 已按两条既定讲法调整组织：①不再强调初赛成绩锚点；②不再讲"提交线/展示线双轨隔离、不改写 solver.py"等防御性叙事，全程按**一个统一的智能调度 Agent 系统**来讲，安全机制按其本身价值（可靠性/灰度信任）表述。

---

## 〇、统一叙事总纲（四个模块的关系）

**一句话主线**：这是一个会自己解题的调度 Agent 系统——**看清场景（感知分诊）→ 排兵布阵（策略池路由）→ 谁行谁上（试跑+确定性评审）→ 到点交卷（anytime 限时输出）**，还会**自己写新策略在沙箱里赢得信任（自进化）**、**跨场景越用越准（长期记忆）**，全过程**每一步都有事件为证（可观测）**。

四个模块的讲述递进关系（一层比一层"活"）：
1. **系统总览** = 地图：先让评委看清整个机器长什么样、数据从哪进从哪出；
2. **Agent Harness** = 底座：模型之外的一切工程——约束、沙箱、护栏、账本；
3. **Loop Engineering** = 发动机：四层循环让系统自己驱动迭代，人不在环里；
4. **长期记忆** = 成长：经验跨场景积累，系统今天比昨天聪明。

建议页数：总览 1 页 / Harness 3 页 / Loop 2 页 / Memory 3 页，机制主体共 9 页。

---

## 一、系统总览（1 页）

### 1.1 机制到底是怎么运转的（你要先讲懂自己）

**端到端一条流水线**（求解一个用例的完整生命周期）：

1. **输入**：TSV 候选表，每行是一条"任务组-骑手"候选：`task_id_list \t courier_id \t total_score \t willingness`（合单组由题目预生成）。逐行解析成六元组，汇总任务全集（solver.py:20-32）。
2. **感知分诊**：用三个统计量——任务数 L、骑手数 d、平均意愿 e——打出场景档位：tiny（≤8 任务）/ small（≤15）/ medium / large（≥40）/ **scarce**（骑手数≤任务数，运力稀缺）/ **low-willingness**（平均意愿<0.27 的低接单意愿场景）（solver.py:33、tools/agent_trace_demo.py:102-122，两处同一套阈值）。**档位决定后面整条策略路由**。
3. **多策略解池**：按档位把 **8 类策略生成器**的产出装进解池——单任务多派贪心 / 列生成+分支定界 / 不相交贪心+补派 / 合单势能匹配 / 稀缺档枚举+最小费用流 / 精英列重组+束搜索打包 / 低意愿多起点扰动 / 官方贪心（恒在，兜底）（代码锚点 solver.py:295/367/924/963/1347/435/282/1661）。解池末尾**无条件**放入官方贪心兜底解（solver.py:122）——这意味着开跑 0.1 秒后手里就永远有合法解。
4. **统一评审（一把尺）**：所有候选解用同一个**期望成本**函数打分：一个任务组派给 J 个骑手，J≤12 时精确枚举 2^J 个"谁接谁不接"子集按意愿概率加权；J>12 切换 Poisson-binomial 精确 DP（O(J²)，结果与枚举逐位一致）；**无人接单/未覆盖任务每单罚 100 分**；骑手复用/任务重复直接记 +∞（solver.py:318-353、1647-1660）。评估结果进 25 万条上限的缓存（solver.py:16）。**因为罚 100 远大于每单典型分（约 16），"先保接单率、再压分数"从数值结构里自动涌现，不需要写任何 if-else 优先级。**
5. **anytime 改进链**：对当前最优解跑一长串邻域改进算子（局部微调 → 费用流全局重排 → 窗口 ALNS 砸块重装 → 列交换 → 低意愿档延迟接受"以退为进"），**每一步必须"新成本 < 旧成本 − 1e-9"才替换**，随时可被时钟打断交卷（solver.py:127-191）。
6. **限时交卷**：内部软截止 **8.7 秒**（为 10 秒硬限留 1.3 秒安全边际；硬稀缺档放宽至 8.85 秒，solver.py:20,43），到点输出 best-so-far。

**Agent 决策闭环（系统的"大脑"，autosolver_agent/system.py:371-728）**：六个角色围着上面这条流水线转——
- **Perception**：解析+画像（5 维特征：tasks/couriers/rows/avg_willingness/has_bundles）；
- **Planner**：两层策略路由——初始轮固定 3 个起手（贪心 0.1s / 单任务多派 0.2s / 不相交贪心 0.45s），按档位再加 1 个；自适应轮按上一轮反馈换向（system.py:326-368）；
- **Executor**：**10 个策略试跑器**（_run_* 函数，system.py:280-323），每个带自己的时间片（0.1s→8.7s），串行试跑；**试跑器就是求解内核策略函数的薄封装——Agent 的十只手就是内核的十套打法**；
- **Critic**：确定性评判器（非 LLM）——合法 且 成本 < 当前最优 − 1e-9 才接受（system.py:504-505）；
- **Controller**：轮次与预算护栏（最多 3 轮、临近截止停止新试跑、全失败自动回退完整求解器）；
- **Memory**：策略注册表 + append-only 事件日志，跨场景召回复用。

每个动作 emit 一条带时间戳的事件（共 **17 种**事件类型），经 SSE 实时推给浏览器，落到**调度工作台六个页面**（实时推理/双屏对比/决策过程/长期记忆/订单池/骑手运力）。**32 个测试文件**五类把守（求解正确性/评估一致性/Agent 四能力/记忆演化/前端契约）。

### 1.2 PPT 怎么排这一页

一张架构图，纵向四层 + 右侧一条贯穿的"事件流"：

```
┌─ 感知层：TSV 解析 → 场景画像（L/d/e → 6 档分诊）
├─ 决策闭环层：Perception → Planner → Executor(10 试跑器) → Critic → Controller  ⟲
├─ 求解内核层：8 类策略生成器 → 期望成本一把尺 → anytime 改进链 → 8.7s 交卷
└─ 成长层：自进化（三道门）+ 长期记忆（Read/Write/Reflection）
   右侧纵贯：17 种事件 → SSE → 工作台六页（事件即证据）
```

**口播（30 秒）**："这个系统一句话：先看清场景、再排兵布阵、谁行谁上、分数说话、到点交卷。上面是 Agent 决策闭环，中间是可插拔的策略内核，底下是让它成长的自进化和记忆——每一步都变成事件实时推到工作台，评委看到的不是动画，是真实代码执行的回放。"

### 1.3 这一页的关键数字

| 数字 | 内容 | 出处 |
|---|---|---|
| 6 档 | 场景档位（tiny/small/medium/large/scarce/low-willingness） | agent_trace_demo.py:102-122 |
| 8 类 | 策略生成器"族" | solver.py 八处锚点 |
| 9 条 | Agent 策略目录 strategy_catalog | system.py:127-135 |
| 10 个 | 策略试跑器 _run_* | system.py:280-323 |
| 一把尺 | 期望成本：2^J 枚举 / DP，罚 100，缓存 25 万 | solver.py:318-353,16 |
| 1e-9 | 全系统统一的严格改进阈值 | system.py:505、evolution.py:145、solver.py 多处 |
| 8.7s | 内部软截止（留 1.3s 边际） | solver.py:20 |
| 17 种 | 事件类型（SSE 实时推流） | system.py:409-693 |
| 6 页 | 工作台页面 | 产品说明文档 |
| 32 个 | 测试文件（28 顶层 + 4 能力测试） | tests/ 实数 |

### 1.4 红线与坑

- **8/9/10/13 四个数字轴不同**，必须主动定口径：8 = 生成器"族"（归纳）、9 = 策略目录条数、10 = 试跑器封装数、13 = 外部插桩工具里的策略函数数（agent_trace_demo.py:19-33）。评委逐个数函数会数出 13——主动说"族 vs 目录 vs 封装"三口径。
- 策略试跑是**严格串行的 anytime 逐个试跑**，没有并行/线程——别说"齐发"“并行"。
- "10 秒限时"的准确说法：内部 8.7s 软截止 + 分层安全垫，**不要说"用满 10 秒"**。
- 演示前检查启动路径：README 里写的是旧目录，当前实际是 `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比`；本地复测用 `python3 _bench.py solver.py 1`（别跑到 solution.py，那是另一条旧入口）。

---

## 二、Agent Harness（3 页）

**模块定调**：Agent = Model + Harness。Harness = 模型之外的一切工程。这个模块回答四个问题：**喂什么（上下文）、怎么动（工具+沙箱）、不许错（护栏+验证）、留什么（状态+观测）**——每一问都有代码级答案。

### 2.1 第一页：Harness 四问全景

**全局硬约束**：官方 10s 限时（内部 8.7s 截止）｜import 白名单｜禁网络/文件/子进程｜固定随机种子（全部 RNG 种子化，sha256 派生，同输入必同输出）。

**四泳道**：

| 泳道 | 内容 | 代码锚点 |
|---|---|---|
| 喂什么 · Context | 11 维场景特征编码 + 档位画像；每轮只注入 Top-K 相似经验，不搬全量历史 | server.py:1709-1721（11 键）、memory_engine.py:61（Top-5） |
| 怎么动 · 执行 | 策略生成走固定接口 propose()（模板引擎，LLM 生成器可插拔）；10 个试跑器是模型触达求解世界的全部 API；AST 静态安检 + 限时沙箱 | evolution.py:309、system.py:280-323 |
| 不许错 · 验证护栏 | 确定性 Critic（期望成本一把尺 + 质量门）；三层超时护栏；多层回退兜底 | system.py:504、evolution.py:145 |
| 留什么 · 状态观测 | 策略注册表 + append-only 事件日志（JSONL+文件锁+原子写）+ 25 万条评估缓存；17 种事件 SSE 全程可回放 | evolution.py:54-56、memory_engine.py:55,310-312 |

**右栏观点**：每个组件都编码一个"模型做不到什么"——守不住时→护栏；不能自证→Critic；记不住→状态账本；看不见→事件流。

**口播（15 秒）**："Agent = Model + Harness，模型之外的一切都是工程。四问落地：喂什么、怎么动、不许错、留什么。"

### 2.2 第二页：三道安全门——机器生成的代码如何赢得信任（灰度信任）

**痛点定调**：系统会自己生成新策略代码——但机器写的代码一行都不能直接信。我们的做法：**把"信任"本身做成流水线**，新代码像新员工一样逐级赢得话语权——相当于策略的灰度发布。

**生成**：按档位选模板生成策略文件（id 如 gen_scarce_v003），固定接口 `propose(candidates, all_tasks, deadline, helpers)`——签名不对直接拒（evolution.py:107-109）。

**三道门流水线**（任一门失败 ⇒ 立即回退 + 留痕）：
1. **AST 静态安检（毫秒级，行动前预防）**：解析语法树逐节点校验——import 白名单仅 6 个标准库（collections/heapq/itertools/math/random/time）；禁 7 个危险调用（eval/exec/compile/open/globals/locals/__import__）；禁 5 个危险根对象（os/pathlib/socket/subprocess/sys）；**while 循环一律拒绝**（evolution.py:15-17,314-315）。
2. **限时沙箱试跑（秒级，行动后纠偏）**：≤0.15s 时间片内跑小样例（system.py:553），超时（+0.02s 宽限）即判负，输出形状不对即毙（evolution.py:116-158）。
3. **质量门（同一把尺）**：期望成本必须 ≤ 基线 − 1e-9——"差不多"不放行（evolution.py:145）。

**通过 → 晋升可复用**：写入策略注册表，状态机 draft → sandboxed → candidate；后续相似场景按画像相似度召回（相似度满分 9：档位相同 +4、合单形态相同 +1、任务/骑手/行数比值各 ≤1、意愿差 ≤1，evolution.py:269-290），replay 复核（≤0.12s、每轮最多 2 条）赢得战绩后继续晋升 trusted → promoted。
**失败 → 留痕退场**：写审计事件 removed_from_active_pool，工作台可回放"谁在哪道门被拒、为什么"——**失败不是终点，是写进账本的负样本**。

**口播（25 秒）**："最大胆的创新是让系统自己生成策略代码，最谨慎的工程是没有一行生成代码靠'信任'上场——静态安检、限时沙箱、质量门三道门，败者留痕退场，胜者逐级晋升，像灰度发布一样赢得话语权。"

### 2.3 第三页：可靠性三件套——不失约 · 可回放 · 全程可见

**时间护栏（为什么要三层：一层闹钟会被长算子穿透）**：全局 deadline 8.7s → 每算子子预算（每个策略/修复器拿 min(自己的时间片, 全局剩余−安全垫)）→ 循环内高频检查（改进链每步查表，随时可中断）→ anytime 兜底（解池 0.1s 起恒含合法贪心解，交卷即取当前最优）。**回退链条：任何失败路径最终都落到恒在的合法解——最坏情况是退化为贪心，而不是崩溃或非法。**

**状态账本（为什么只增不改：出问题能回放到任何一轮）**：策略注册表（得分滚动平均+风险+职级）；append-only 事件日志（JSONL + 文件锁 + tmp 写入后原子替换）；25 万条评估缓存；受严格守卫保护的已知场景缓存解（任务/骑手/每行存在性精确校验后才触发，未命中自动回实时链路——"历史经验固化进热路径"）。

**可观测**：17 种事件（perception → attempt_start/result → best_update → adapt → evolution_promote/rollback → final）SSE 实时推流 → 工作台六页渲染；契约测试锁死事件流格式（test_web_agent_demo.py:1093）。**确定性执行**：固定/派生种子 ⇒ 同输入必同输出，问题可调试、结果可复现。**失败详细、成功安静**：只有失败带全文错误进入下一轮上下文。

**口播（10 秒，快讲页）**："可靠性三件套：不失约——三层时间护栏；可回放——只增账本；全程可见——事件推流。"

### 2.4 红线与坑

- **"沙箱"不是进程级隔离**：策略在主进程内加载执行，超时是事后判定、无法抢占中断——所以才在 AST 层面**直接禁 while 循环**防死循环。准确说法："AST 白名单 + 接口契约 + 协作式限时的三层轻量沙箱"。被问"为什么禁 while"，这就是标准答案。
- 策略生成**默认是确定性模板**（按档位换排序键），LLM 生成器是可插拔位——别说漏成"模型自己写任意代码"。
- 演示预算的 10s（Agent 循环的钟）与求解内核的 8.7s（交卷的钟）是**两个独立计时器**，别混为"同一个 10 秒"。
- 安全门/沙箱按**可靠性与灰度信任**的价值讲，不要挂靠"保护提交件"的叙事（既定讲法调整）。

---

## 三、Loop Engineering（2 页）

**模块定调**："我不再 prompt，我写循环，循环去 prompt。"——把人从环里拿出来，让系统自己驱动迭代。赛题四项能力恰好各落一层循环，逐条可指认。

### 3.1 第一页：四层嵌套循环

| 层 | 名字 | 反馈信号 | 干什么 | 赛题对应 |
|---|---|---|---|---|
| L1 | 策略执行环 | 分数与耗时 | 感知→规划→试跑→观察→下一步（初始轮 3-4 个策略，各带时间片） | 自主策略探索 |
| L2 | Generator-Critic 验证环 | 合法性 + 严格改进 | Critic（**确定性评判器，非 LLM**）一把尺验分，合格才收 | 自动评估与筛选 |
| L3 | 自适应换向环 | 档位归因 | 低意愿→转列搜索；稀缺→转费用流；覆盖不全→转稀疏覆盖——**换向必须带原因** | 迭代改进循环 |
| L4 | 跨轮学习环 | 历史经验相似度 | 记忆 Read → 改变策略排序 → Write → Reflection 提炼，跨场景越用越准 | （超纲加分项） |

**层间咬合**：L1 每轮产出分数与耗时 → L2 判收或拒 → L3 依据档位信号决定下一轮打法 → L4 把整局经验写进记忆、下一个场景生效。

**停止条件（每层都可验证）**：时间截止 · 轮次上限 · "合法且 cost 严格更优"才替换 · 全失败自动兜底并发 fallback 事件。

**一轮真实事件流（工作台可见）**：perception → evolution_recall → round_start → attempt_start → attempt_result → best_update → adapt → evolution_promote/rollback → final。

**口播（25 秒）**："四层循环各管一件事：执行、评审、换向、学习。赛题要求的自主探索、自动评估、迭代改进逐条落位，每层都有自己的反馈信号和停止条件——人不在环里，证据都在。"

### 3.2 第二页：验证与换向（循环的质量在反馈是否结构化）

**Generator 侧（大胆）**：10 个试跑器按时间片阶梯依次上场——贪心 0.1s / 单任务多派 0.2s / 不相交贪心 0.45s / 稀疏覆盖 0.55s / 低意愿单列 0.6s / 合单势能 0.65s / 低意愿全局列与稀缺 K2 各 0.7s / 稀缺费用流 0.8s / 完整求解器 8.7s（system.py:328-367）。生成可以大胆——因为把关的不是它自己。

**Critic 侧（冷酷，三步）**：①合法性校验（骑手复用/任务重复/行不存在 → 记 +∞）；②期望成本统一打分（与内核同源同一个函数）；③严格改进阈值 cost < best − 1e-9——"差不多"不算改进。**失败也是数据**：异常不炸循环，而是变成一条带失败原因的 attempt_result 事件，成为下一轮换向的输入（system.py:488-502）。

**换向规则表（不是碰运气重试）**：低意愿信号 → 列搜索+多起点扰动+鲁棒选优；稀缺信号 → K2 列生成/最小费用流全局补全；覆盖不全 → 稀疏覆盖修复（先接住单再谈成本）；全失败 → 回退完整求解并发 fallback 事件（system.py:339-368）。

**防 doom loop**：轮次上限 · 每策略时间片 · 同信号不重复试同方向——反复撞墙不叫迭代。

**三个反模式（右栏）**：自评偏乐观 → 生成与评判彻底分离、Critic 可复现；盲目重试 → 每次换向都带"为什么"；完成标准模糊 → "写完了"不是标准，"cost < best − 1e-9"才是。

**口播（15 秒）**："Generator 大胆试，Critic 冷酷收；被拒必须归因、换向必须带原因，绝不盲目重试。"

### 3.3 红线与坑

- **"最多 3 轮"是护栏，实际最多 2 轮入队**（初始轮 + 一次自适应轮，system.py:549）——讲"迭代改进"的证据是 adapt 事件与第二轮换向，别说"跑满 3 轮"。
- **Critic 是确定性评判器（非 LLM）**——这是加分点（评判可复现），主动讲。
- 低意愿档的"三种行为假设鲁棒混合"（0.45/0.45/0.1 加权 + 0.15×最大遗憾）只在**近平手时做裁决**，最终打分永远只认期望成本一把尺——被问"到底几把尺"按这个口径。
- "9 种组合"的可答辩锚点 = strategy_catalog 恰 9 条（system.py:127-135）；被追问时承认"9 是策略目录条数，运行时按档位路由其中子集"。

---

## 四、长期记忆（3 页）

**模块定调**：记忆不是日志，是**决策部件**——它的输出不是"想起了什么"，而是"这一轮换用哪个算法"。

### 4.1 第一页：三层记忆——日记 · 画像 · 直觉

**原始决策流（求全但嘈杂）**：每轮胜负记录 + 11 维场景特征 + 冲击事件；全天 64 轮 × 每轮 3 条（召回/回写/提炼）= **192 条/天**。直接喂给决策 = 噪声。

**三层状态板（由 Reflection 持续维护）**：
- **情景记忆 · 日记（求全）**：append-only 事件流，192 条/天，只增不改、历史永在；
- ↓ 签名聚类·归纳 ↓
- **语义记忆 · 画像（求准）**：**12 类场景画像 + 3 类实体画像**（骑手供给/商圈压力/订单风险）——"雨天午高峰运力紧"是一类，不是一堆日志；
- ↓ 状态机筛选·固化 ↓
- **隐性记忆 · 直觉（求快）**：**7 条全局策略先验**——"这类场景优先用某算法"，决策时零思考成本。

**决策侧读取（低噪声）**：每轮只注入 Top-K 相似经验 + 相关画像 + 先验，四路召回各 Top-5——**192 条全量从不进决策窗口**。

**口播（20 秒）**："记忆三层：日记求全（192 条）、画像求准（12+3 类）、直觉求快（7 条先验）——嘈杂的经历经两次提炼，才成为能用的经验。"

### 4.2 第二页：召回如何真正改变一次派单（机制核心页）

**五步链路**：
1. **场景编码**：当前轮 11 维特征 → 场景签名（时段｜天气｜拥堵｜运力｜冲击）；
2. **相似度计算（连续分数）**：8 维加权、权重和=1——**场景 ID 0.22 / 场景类型 0.18 / 天气 0.14 / 订单压力 0.14 / 运力压力 0.10 / 接单意愿 0.08 / 交通画像 0.08 / 拥堵水平 0.06**（memory_engine.py:571,575；类别维精确匹配、数值维按 1−|差| 渐变）——"像多少"而不是"见没见过"；
3. **四路召回各 Top-5**：情景/决策/策略/进化四类记忆包，只注入最小相关集；
4. **memory_boost 加成**：`boost = 相似度 ×（历史均分 × 0.22 + 职级加成）`，职级加成 **promoted 7.0 / trusted 4.0 / candidate 2.0 / draft 0.5**（memory_engine.py:409-413）——人话：**越像、越资深的经验，话语权越大**；
5. **重排并采纳**：boost 加进候选算法本轮实测分重排——排名第 1 的方案被真正派出，结果立即 Write 回写（仅 read-write 模式落盘）。

**Read 在评分之前、Write 在选择之后**——记忆是决策前置输入，不是事后日志（compare_engine.py:173-181,217-218）。

**策略"职级"状态机（晋升与退场都有硬阈值）**：candidate 实习 → trusted 转正（尝试 ≥2 且被选中 ≥1）→ promoted 骨干（被选中 ≥2 且均分 ≥68，0~100 归一化胜负分）；退场 rolled_back（尝试 ≥3、零被选、均分 <45——移出活跃池但留痕可审计）（memory_engine.py:450-467）。

**两个硬证据**：
- **单测翻转**：promoted 记忆的加成足以翻转"本轮采纳哪个算法"且实际派单方案随之改变（tests/test_simulation_memory.py:44-77，可现场跑通）；
- **消融实测**：15 组同种子平行世界 × 24 轮 = 360 决策轮，开记忆合计**多送 30 单（+2.9%）、积压少 30 单、72% 轮次决策被记忆改变**（v10 P17 定稿口径）。

**口播（25 秒）**："记忆怎么改变派单：8 维相似度召回 Top-K，boost 加进实测分重排算法——越像、越资深的经验话语权越大。有单测证明 promoted 记忆足以翻转'本轮用哪个算法'，消融实测 72% 轮次的决策被记忆改变。"

### 4.3 第三页：全天学习效果（成长曲线页，页面实时演示）

**四级经验状态（64 轮全天决策的分布 = 1 / 2 / 9 / 52）**：
- 冷启动（当天零历史，仅首轮 07:10）：1 轮；
- 低相似借鉴（0<最高相似<0.5，旧经验只当辅助）：2 轮；
- 高相似迁移（≥0.5，旧经验当主力直接搬用）：9 轮；
- 同景复遇（签名完全命中）：52 轮。

**学习效果锚点（以页面实时显示为准）**：记忆参与 61 轮（9+52），贡献全天节省 433 分钟的 **99%**；记忆主导轮的单轮均省是低经验轮的 **×4.0**；置信度 **0.57 → 0.92**。

**成长弧线（讲一个具体故事）**："11:57，系统遇到一个只有 0.36 相似度的新场景——旧经验只匹配上'时段+运力'两个维度，只能低相似借鉴，这一轮只赚了 +4.8 分钟；46 分钟后的 12:43，'午高峰+雨+高拥堵+缺人+双冲击'这个场景第二次复遇——经验直接命中，单轮 +123.1 分钟，全天最佳，置信度冲到 0.92。**从摸着石头过河到轻车熟路，就发生在同一个中午。**"

**页面组件**（现场演示顺序）：证据瓷砖 → 全天学习曲线（累计节省+置信度双面板，13 秒回放动画）→ 场景经验库复用矩阵（首遇沉淀→复遇召回弧线）→ 单轮召回链路四段流水线（场景编码→相似经验召回→决策执行→结果回写）。

**口播（25 秒）**："全天 64 轮决策：只有开局 3 轮是低经验状态，之后 61 轮都有记忆参与、贡献了全天节省的 99%。最能说明'学习'的是这个对照：11:57 低相似借鉴只赚 4.8 分钟，12:43 同景复遇单轮赚 123 分钟——系统上午交的学费，中午就变成了利润。"

### 4.4 红线与坑

- **机制页与效果页的数字来自两条链路，各讲各的**：机制页（8 维权重/boost/状态机/单测/消融）用 memory_engine+compare_engine 的真实机制；效果页（64 轮/1-2-9-52/433 分钟/×4.0）用工作台页面实时派生的数字并标注"以页面实时显示为准"。两套都真实，但**不要把效果页数字说成机制层消融结果**。
- **消融的正确讲法**：compare 引擎消融 memory_boost + 单测翻转 + "15 组×24 轮"实测；day_simulation 的 memory_mode=off 只是不生成叙事事件、派单结果不变——**绝不能当消融讲**。
- 精确底值（被追问时）：99% 实为 98.8%、×4.0 实为 3.97、0.57→0.92 实为 0.5733→0.9233；12:43 页面文案是"同景第 2 次复遇"。所有页面数字随 seed/订单规模/骑手数变化，PPT 不写死。
- 8 维权重映射别写错：**traffic_profile 是 0.08、order_pressure 才是 0.14**（排序后 0.22/0.18/0.14/0.14/0.10/0.08/0.08/0.06）。前端展示用的五维权重（0.18/0.14/0.14/0.18/0.14+同景 0.22）是引擎 8 维的归并口径——**PPT 上二选一并注明口径，别并排放两组对不上的数**。
- 术语红线：只用 情景/语义/隐性记忆、Read/Write/Reflection、Reflexion 反思回写、检索增强决策、记忆注入时机、Generator-Critic；不用 ACE/consolidation/decay/procedural memory；不用"全新/从零/清零"等绝对化词。
- `长期记忆系统设计说明_v2.md` **已不存在**（项目详解文档仍引用它）——PPT 与讲稿不要引用该文件名，一律以代码和《美团AutoSolver项目详解.md》为准。
- log-v5-claude-重构版 里是另一套三层记忆（Working/Episodic/Semantic），与本页面不是同一系统，别混讲。

---

## 五、跨模块数字口径速查（全部经代码回查确认）

| 口径 | 值 | 出处 |
|---|---|---|
| 策略池 | 8 类生成器 → 9 条策略目录 → 10 个试跑器 | solver.py 多处 / system.py:127-135 / :280-323 |
| 时间 | 官方 10s / 内部 8.7s（硬稀缺 8.85s）/ 1.3s 边际 / 0.1s 起恒有合法解 | solver.py:20,43,122 |
| 评估 | 2^J 枚举（J≤12）/ O(J²) 精确 DP / 罚 100 / 缓存 25 万 / 阈值 1e-9 | solver.py:318-353,16 |
| 安全门 | 白名单 6 模块 / 禁 7 调用 / 禁 5 根对象 / 禁 while / propose 四参数 | evolution.py:15-17,107-109,314-315 |
| 沙箱 | 试跑 ≤0.15s / replay ≤0.12s×2 条 / 超时宽限 0.02s | system.py:553,612,621 |
| 循环 | 四层 L1-L4 / 3 轮护栏（实际 2 轮入队）/ 17 种事件 | system.py:452,549 |
| 记忆权重 | 8 维和=1：0.22/0.18/0.14/0.14/0.10/0.08/0.08/0.06 | memory_engine.py:571,575 |
| boost | 相似度×(均分×0.22+职级 7/4/2/0.5) | memory_engine.py:409-413 |
| 状态机 | candidate→trusted(≥2 试且被选)→promoted(被选≥2 且≥68)/rolled_back(≥3 试零选<45) | memory_engine.py:450-467 |
| 漏斗 | 192 条情景 → 12 类场景+3 类画像 → 7 条策略先验 | 前端实时派生，实测复现 |
| 四级分布 | 冷启动 1 / 低相似借鉴 2 / 高相似迁移 9 / 同景复遇 52（共 64 轮） | day_replay_frontend_v2.py:5660-5673 实测 |
| 学习效果 | 61 轮贡献 99%（实 98.8%）/ ×4.0（实 3.97）/ 置信度 0.57→0.92 / +4.8→+123.1 | 前端 memoryEvidence 实测 |
| 消融 | 15 组×24 轮=360 决策轮：+30 单(+2.9%) / 积压−30 / 72% 轮次被改变 | v10 P17 定稿口径 |
| 测试 | 32 个测试文件（五类把守）；记忆翻转单测可现场跑 | tests/ 实数、test_simulation_memory.py:44 |
