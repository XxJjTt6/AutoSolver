# 合单配送（顺路带多单）· 后端真算设计方案

> 目标：让一个骑手能像真实外卖那样**顺路带多单**（同商家/近商家/顺路），而不是现在的"一单一送"。
> 红线：**后端真算、前后端一致、绝不前端编造**。合单是算法的真实派单决策，地图画的 = 后端算的距离/时长。

---

## 1. 核心模型：统一「边际插入 + 容量」的批次派单

不做两套规则（同商家 / 顺路各一套），而是**一个统一模型**——同商家合单是"顺路合单"的特例（取餐点重合、绕路≈0）。

- **骑手 = 背包**：最多同时带 `CAP=3` 单（用现有 `courier.capacity`，真实量级）。
- **批次形成（每时段内边处理边形成）**：订单流里，新订单 O：
  1. 在当前"正在装配"的批次里找一个**可插入**的：取餐点是同商家、或商家间距 ≤ `NEAR_MERCHANT_M`（约 350m）；且目的地方向不太绕（插入后全批**总道路时长增量** ≤ 阈值）；且**插入后全批都不超 deadline**；且未超 `CAP`。
  2. 找到 → O 并入该批；找不到 → O 自成新批。
  3. **这一步就是算法的真实决策**：我方按"综合边际成本（时间+超时风险+负载+未来压力）最小"择优并入；基线（最近贪心）只并"同一商家、同一时刻"的单（朴素批），其余一单一派。→ 我方合得多、基线合得少，差距**真实来自智能，不是写死乘子**。
- **批次路线（真实路网）**：骑手当前位置 → 取餐点（去重、最近序）→ 各客户（最近序）。用 `road_routing` 真实路网算每一段。
- **每单结果**：`finish_at_s` = 它的客户被送达的真实时刻；该单的距离/ETA/成本从**真实批量路线**里算（前后端同源）。

## 2. 与现有仿真的接法（不改 day_simulation.py 原文件，走 monkeypatch）

`road_routing_patch.apply()` 追加替换 `ds._simulate_day_algorithm`：把"每单串行占满骑手"改成"批次派单"：

- `_CourierPlan` 逻辑上增加：当前批次 `chain`（未送达订单）、`available_at_s` = 批次全部送完的时刻、`position` = 最后一个送达点。
- 处理顺序仍是"逐时段、逐订单"，但**批次在被记录前先定稿**（避免"加一单改前面单 finish"的回填问题）：
  - 维护"本时段在装批次"列表；每个 O 尝试并入或新建；批次满 `CAP` 或时段结束即"封批"并**分配给最优骑手**（我方综合分 / 基线就近）。
  - 封批后：为批内每单产出一条 `_AssignmentRecord`（真实 finish、真实距离），骑手 `available_at_s` = 批次结束、`position` = 末点、`assigned_count += 批大小`。
- 复用现成的 `_metrics_from_records / _courier_position_snapshots / _slice_decision_summary / _should_emit_frame`——只要 record 是真实的，准时率/均时/成本/负载等指标自动正确。

## 3. 路线载荷与前端（多点串行）

- **每单 overlay**：`polyline` = 骑手 → 该批取餐点序列 → 一直到"该单客户"的真实路网折线；`merchant_index` 保留（第一个取餐点），多单共享的前半段自然重叠（真实如此）。
- **骑手标记**：`R-05 · 带3单`（motion=moving 时若带多单显示单数）。
- **底部卡片**：批内每单各一张（已有卡片体系），头部标注"合单 ×N / 同骑手 R-05"，点/双击索引到该骑手这条链。
- **动画**：骑手沿批次多点路线走，依次点亮"送达 O-a、送达 O-b…"。
- **前端只画后端给的 polyline / finish**，不自己编造顺序或距离。

## 4. 诚实性与公平性（回应"要真实"）

- **两算法同一套物理 + 同一套合单机制**，只是**目标函数不同**：基线用就近/边际距离贪心（合得少、合得糙），我方用综合目标（时间+超时+成本+负载+未来压力，合得多、合得准）。延续我们已确立的"同物理、不同智能、零暗改乘子"原则。
- 合单**不是前端拼的**：是后端派单循环里真实形成批次、真实算路线与时刻；前端渲染后端产物。
- 合单**受 deadline 硬约束**：并入后任一单会超时则不并——不会为了"看起来合得多"牺牲真实性。

## 5. 预期影响（会变的东西，提前说明）

- **数字会再变一次**：合单后我方车次更少、总距离更短、单均成本更低、准时率更稳——**我方优势预计更大且更真实**（新增"顺路合单"这一真实优势维度）。基线基本不变。
- **要重跑路网缓存**：批量路线触及新的取餐/送达腿。
- 我按需微调运营点（订单密度/骑手数），让合单效果自然、可信、不夸张。

## 6. 参数（初值，可调）

| 参数 | 初值 | 含义 |
|---|---|---|
| `CAP` | 3 | 骑手同时最多带单数 |
| `NEAR_MERCHANT_M` | 350 | 可合并的商家间距上限 |
| `BATCH_EXTRA_TIME_S` | 300 | 并入后允许的全批时长增量上限 |
| deadline 约束 | 硬约束 | 并入后任一单超时则不并 |

## 7. 实施与验收顺序

1. 后端批次派单（patch 重写 `_simulate_day_algorithm` 的分配段）→ 验证：出现"一骑手带 2~3 单"的真实批次、指标从真实 record 算出。
2. 路线载荷改多点 + 前端多点渲染/骑手"带N单"/卡片合单标注/索引。
3. 重跑路网缓存（foot）。
4. 全链验证：合单真实存在、前后端距离一致、0 回退、两图都对、新数字合理、六页零报错。
