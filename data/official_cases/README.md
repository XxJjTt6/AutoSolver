# 样例数据

该目录保存随仓库发布的比赛格式样例，用于本地验证求解器的输入解析、合法性和运行时间。

| 文件 | 说明 |
|---|---|
| `large_seed301.txt` | 较大规模的候选任务与骑手输入。 |
| `example_solution.txt` | 对应格式的示例输出，用于理解提交结构。 |

运行一次本地基准：

```bash
python3 _bench.py solver.py 1
```

仓库样例只用于开发验证，不代表比赛完整测试集，也不应被解读为官方成绩。
