# 推理过程可视化改造 · 设计方案 v1

> 目的：回应老师"**没有推理过程 / 结果都是定死的 / 几个界面要同步推理**"的意见。
> 原则：**不推翻现有真实后端**（day_simulation 的算法结果保持真实），只改"呈现方式"——让推理这件事**看得见、边推边出、六视图同步**。

---

## 0. 老师三点意见 → 本方案怎么对上

| 老师意见 | 本方案对应 |
|---|---|
| ① 要有"推理的过程" | 决策过程页改成**流式推理事件**（Planner→Executor→Critic→Controller→Memory→Evolution 逐条冒出） |
| ② "还没推理每一轮结果就有了 / 定死" | **Reveal-on-Reason**：每轮派单结果只在它的推理事件流播完后才落到地图/指标 |
| ③ "几个界面独立，应该同步推理" | 抽一个**全局推理时钟**，六页订阅同一个时钟一起走 |

---

## 1. 核心原则：Reveal-on-Reason（先推理、后揭示结果）

- **现状**：后端一次性把全天 64 轮算完 → 前端沿时间轴回放。跳到第 30 轮，它的结果早就在那 → 观感像"结果预先定死、在放录像"。
- **改造后**：点"推理"= Agent 从当前轮开始逐轮推理，**本轮的候选一个个冒出来 → Critic 逐个判 → 选出 best-so-far → 这时地图上的线才画出来、指标才跳**。
- **关键**：数据仍是后端真实算的（不作假），只是把"呈现顺序"改成"推理在前、结果在后"，从根上消除"预先定死"的观感。

---

## 2. 决策过程页 = 主战场（新形态草图）

```
┌───────────────────────────────────────────────────────────────────────────┐
│  决策过程 · 第 23 轮 / 12:12  午高峰·雨天·高拥堵·降雨+商家爆单                │
├──────────────┬────────────────────────────────────┬────────────────────────┤
│ 轮次时间线    │  实时推理事件流 ▶推理 ⏸ 单步 倍速2x  │  本轮检查器              │
│ ● 21轮 11:42 │  ┌──────────────────────────────┐  │  EVENTS 14  ACCEPTED 2   │
│ ● 22轮 11:57 │  │+0.0s ⬤PLANNER 触发本轮        │  │  ROUND 23                │
│ ▶ 23轮 12:12 │  │  订单压力达阈值，提出4个候选策略 │  │  ─────────────           │
│   24轮 12:27 │  │  [O-045][O-046][O-047]         │  │  当前阶段                 │
│   ...        │  ├──────────────────────────────┤  │  Critic 评审候选          │
│              │  │+1.2s ▦EXECUTOR 调用 risk-aware │  │  当前动作                 │
│              │  │  为高拥堵单构造风险均衡派单     │  │  评审 cost_greedy 候选    │
│              │  │  [R-06→O-045] slice 0.48       │  │  ─────────────           │
│              │  ├──────────────────────────────┤  │  best-so-far ↑            │
│              │  │+2.4s ✓CRITIC 采纳 risk-aware   │  │  分数 0.260 (↑ from .18)  │
│              │  │  best-so-far 更新：0.18→0.26   │  │  ─────────────           │
│              │  ├──────────────────────────────┤  │  Memory 命中              │
│              │  │+2.4s ✗CRITIC 否决 nearest      │  │  复用"午高峰·雨天"经验    │
│              │  │  只顾最近、忽略拥堵，保留备选   │  │  相似度 0.64              │
│              │  ├──────────────────────────────┤  │                          │
│              │  │+3.1s ◆MEMORY 命中历史经验      │  │                          │
│              │  │  注入"雨天优先风险均衡"先验     │  │                          │
│              │  ├──────────────────────────────┤  │                          │
│              │  │+3.6s ◈WRITEBACK 回写3条记忆    │  │                          │
│              │  └──────────────────────────────┘  │                          │
│              │  ▼ 事件流播完 → 本轮结果揭示：      │                          │
│              │    节省 +23.2 分钟，派单 6 个动作   │                          │
└──────────────┴────────────────────────────────────┴────────────────────────┘
```

**说明**：
- 中栏事件卡**逐条"叮"地出现**（不是一次性铺满），每条带 `+X.Xs` 相对时间戳、角色标签、一句话、涉及的订单/骑手/候选 chip。
- 关键戏剧点：**Critic 采纳/否决**（best-so-far 更新时高亮跳分）、**Memory 命中**（注入历史先验）。这就是"能看到 Agent 在想"。
- 右栏检查器：EVENTS / ACCEPTED / ROUND 计数 + 当前阶段 + 当前动作 + best-so-far 实时跳（对齐初赛截图那套）。
- 事件流播完，才在底部揭示"本轮节省 X 分钟 / 派单动作"。

**数据来源（几乎全现成）**：`workbench.decisions[i]` 里已有 `trigger_reason / filtering_process / scoring_process(候选+分数) / final_actions / abandoned_actions / result_writeback / round_result`。**缺的只是"把它们拆成带时间戳的事件、按序播出来"**，不需要重造数据。

---

## 3. 同步推理时钟（贯穿六页）

**现状**：`seekInferenceTime` 已是全局时间，但每页各有播放/暂停、互不联动（这正是老师说的"几个界面独立"）。

**改造**：抽一个全局 **ReasoningClock**（单一 播放/暂停/倍速/进度/轮次），六页都订阅它：

```
                     ┌─────────────────────────────┐
                     │   ReasoningClock (全局唯一)   │
                     │  ▶推理  轮次 23/64  阶段 Critic│
                     └───────────────┬─────────────┘
              broadcast tick（当前轮/当前事件/是否揭示结果）
      ┌──────────┬──────────┬────────┴────┬──────────┬──────────┐
   决策事件流    实时地图     长期记忆      订单池     骑手运力
   (推理主视图)  (结果随推   (命中/回写时  (状态随   (负载随
                 理完才画)   对应行点亮)   时钟更新) 时钟更新)
```

- 顶部放一条**全局推理条**（哪一页都能看到）：`▶推理 | 轮次 N/64 | 当前阶段 | best-so-far`。**切页不打断**，时钟继续走。
- 一个主控管全局：`推理 / 暂停 / 单步 / 倍速 / 拖到某轮 / 重新推理`。
- 当推理推进到第 N 轮：决策页放该轮事件流、地图在该轮推理完后画出结果、记忆页对应场景行/曲线点同步点亮、订单/骑手状态同步更新。

---

## 4. 交互规格（关键几条）

| 操作 | 行为 |
|---|---|
| ▶ 推理 | 从当前轮开始，逐轮"推理→揭示结果"，六视图同步前进 |
| ⏸ 暂停 | 停在某事件，检查器/地图定格 |
| ⏭ 单步 | 推进**一个推理事件**（讲解时看清 Critic 怎么否候选） |
| 拖动/跳轮 | 跳到第 K 轮：前 K-1 轮按"已推理完"呈现，第 K 轮停在其推理完成态 |
| 倍速 | 控制事件流速度（讲解慢、过场快） |
| ↻ 重新推理 | 回到 07:00 清空，从零开始逐轮推——真正的"结果不预先有"，直接堵老师"定死"那句 |

---

## 5. 可行性 & 复用（为什么不贵）

| 要素 | 现状 | 改造工作 |
|---|---|---|
| 每轮推理数据 | 已全有 `workbench.decisions` | 拆成带时间戳的事件序列（映射函数） |
| 全局时间轴 | `seekInferenceTime` 已是全局 | 升级成 ReasoningClock，页面改为订阅 |
| 结果揭示 | 立即全在 | 加"本轮结果 gate"：事件流播完才落图/跳指标 |
| 真实推理引擎 | `autosolver_agent/run_case_agent` 还在项目里 | 可选彩蛋：单案例现场真跑一遍 |
| 六页联动 | 各自播放 | 统一订阅一个时钟 |

---

## 6. 分阶段实施（每阶段可独立验收，随时可停）

- **P0 · 半天｜先给老师看得见的东西**
  决策过程页把静态①-⑥卡改成**带时间戳、逐条冒出的事件流** + 右侧实时检查器计数。只动这一页，视觉上立刻"有推理过程了"。
- **P1 · 约1天｜Reveal-on-Reason**
  本轮地图结果/指标改成"事件流播完后才揭示"；加"重新推理"从零开始。彻底回应"结果定死"。
- **P2 · 1~2天｜同步时钟**
  抽 ReasoningClock，六页订阅同步；顶部全局推理条，切页不断。回应"几个界面要同步"。
- **P3 · 可选彩蛋｜现场真跑**
  接 `run_case_agent`，选一个用例**现场真算一遍**事件流，作为"这不是回放、引擎真在算"的铁证（呼应老师"推理了吗 / 评委知道吗"）。

---

## 7. 不做什么（范围边界，避免过度/弄巧成拙）

- **不推翻**现有后端真实算法、双屏对比、记忆页——保留。
- **不伪造**耗时/假 loading 来"装作在算"——那才是真造假。事件时间戳用真实决策的相对时序（或 P3 引擎真跑时长）。
- 上一轮审计发现的"**写死每算法乘子造优势**"（速度×1.06 等）是**另一件事**，本方案不含，建议单独处理（否则和"数据真实"的说法自相矛盾）。

---

## 8. 请老师确认的三个问题（对齐需求，避免白干）

1. 你要的"推理过程"，是不是就是这种**能看到 Planner/Executor/Critic 逐步 + 候选被接受/拒绝**的事件流？
2. "同步推理"是不是指**一个播放键让地图 + 决策 + 记忆一起走**？
3. 要不要 **P3 那种"单案例现场真跑"**作为"不是回放"的铁证？

---

*本文件为设计草案，确认方向后再进入编码。对应代码：`web_agent_demo/day_replay_frontend_v3.py`（决策过程页 renderDecisionsPage）、`web_agent_demo/day_simulation.py`（决策数据）、`autosolver_agent/system.py`（run_case_agent，P3 用）。*
