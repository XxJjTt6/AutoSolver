# 长期记忆系统设计说明（log-v4-claude-显示对比 当前版）

> 本文档描述本版 Agent 调度系统中「长期记忆」的完整设计与实现：数据怎么存、怎么召回、怎么影响决策、怎么回写，以及前端如何把这套机制可视化成评委看得懂的证据。所有数字与公式均与代码逐一核对过，文中给出代码落点，可直接对照检查。

---

## 0. 一页总览

本系统的记忆分**三层**，自底向上：

```
┌─────────────────────────────────────────────────────────────────┐
│ ③ 可视化层（day_replay_frontend_v2.py · 「长期记忆」页）          │
│    三态记忆状态 / 可借鉴经验池 / 学习曲线 / 场景经验库 /          │
│    召回链路 / 分层提炼 —— 把①②的机制变成可核对的证据             │
├─────────────────────────────────────────────────────────────────┤
│ ② 推演叙事层（day_simulation.py · EvolutionMemoryEvent）        │
│    每个决策帧派生 召回/回写/策略 三类记忆事件，                   │
│    含场景签名、召回案例、learned_rule、置信度 before/after        │
├─────────────────────────────────────────────────────────────────┤
│ ① 记忆引擎层（memory_engine.py · SimulationMemoryStore）        │
│    真实可持久化：JSONL 事件流 + 策略注册表 + 文件锁；             │
│    加权相似度召回 → memory_boost 改变算法排名 → 结果回写状态机    │
└─────────────────────────────────────────────────────────────────┘
```

一轮决策中记忆的完整闭环（Read → 决策 → Write → Reflection）：

```
当前场景特征 ──► 场景编码（元数据过滤 + 加权相似度）
                    │
                    ▼
            四路召回 Top-5（场景/决策/策略/进化记忆）      ← Read
                    │
                    ▼
            memory_boost 抬升候选算法得分 → 重排 → 选算法   ← 记忆注入时机：评分前
                    │
                    ▼
            派单执行，与基线（最近距离贪心）逐轮对比
                    │
                    ▼
            结果回写：running average + 策略状态机晋升/回滚  ← Write（Reflexion）
                    │
                    ▼
            跨轮提炼：场景画像 / 全局策略规则                ← Reflection
```

---

## 1. 记忆引擎层（真实持久化记忆）

**文件**：`/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/memory_engine.py`
**核心类**：`SimulationMemoryStore`

### 1.1 存储结构（Persist）

| 存储物 | 文件 | 形式 | 内容 |
|---|---|---|---|
| 事件记忆（情景记忆） | `memory_events.jsonl` | **append-only JSONL** | 每次对比/决策产生的原始事件，只追加不修改 |
| 策略注册表（策略记忆） | `strategy_registry.json` | JSON 字典 | 每个算法/进化候选一条，含统计量与状态机 |
| 并发锁 | `.memory.lock` | 文件锁 | 多进程读写互斥 |

落盘目录由环境变量 `AUTOSOLVER_MEMORY_ROOT` 控制，默认 `web_agent_demo/.simulation_memory/`（见 `memory_engine.py` 约 L50-55、`server.py` 约 L55）。

**跨会话/跨天**：JSONL 与注册表落盘即持久，服务重启后记忆仍在——这就是"长期"记忆区别于单轮上下文（工作记忆）的工程根据。

### 1.2 记忆条目 Schema

**事件记忆**（`_normalize_memory_event`，约 L504-517）：

```json
{
  "event_id": "sha1 前 16 位",
  "created_at": "UTC ISO 时间",
  "event_type": "scenario_seen | compare_completed | strategy_selected |
                 strategy_rejected | evolution_candidate_generated |
                 strategy_trial | evolution_promoted | evolution_rolled_back",
  "scenario_hash": "场景哈希（16 位，见 1.3）",
  "compare_run_id": "...", "tick_id": "...",
  "features": { "…11 维场景特征…" },
  "algorithm_id": "...",
  "metrics": { "score / coverage / eta / risk …" },
  "decision_summary": "为什么选/弃",
  "privacy": { "secret_free": true }
}
```

**场景特征 `ScenarioFeatures`**（`compare_engine.py` 约 L45-57，由 `_scenario_features` 计算）：
`scenario_id / scene_type / weather / traffic_profile / congestion_level / order_pressure（活跃单数÷可用骑手） / courier_pressure / active_order_count / courier_count / avg_willingness / burst_active`

**策略注册表条目**（`_updated_strategy_entry`，约 L438-468）：

```json
{
  "status": "candidate | trusted | promoted | rolled_back | draft",
  "attempts": 7, "selected_count": 5,
  "avg_score": 71.3, "avg_timeout_risk": 0.18, "avg_no_accept_risk": 0.05,
  "last_reason": "...", "last_scenario_hash": "...", "last_seen": "...",
  "seen_contexts": ["最近 12 条见过的场景上下文（含 features）"],
  "rollback_action": "removed_from_active_pool（仅回滚时）",
  "evolution_type": "…（进化候选才有）", "source_algorithm_id": "…"
}
```

### 1.3 Write：记忆如何形成

触发点：`compare_engine.run_comparison` 结束且 `memory_mode == "read-write"` 时调用 `memory_store.record_compare_result(result)`（`compare_engine.py` 约 L217-218）。

每次对比写入 **3 + N 条事件**：
- 1 条 `scenario_seen`（见过这个场景）
- 1 条 `compare_completed`（这次对比的总结）
- 1 条 `strategy_selected`（胜出算法）+ 每个落选算法 1 条 `strategy_rejected`

同时用 **running average**（`_running_average`）更新注册表里全部候选算法的 `avg_score / avg_timeout_risk / avg_no_accept_risk`，并追加 `seen_contexts`（保留最近 12 条）。

另有进化通路：`evolution_candidate_generated`（注册新策略候选）与 `strategy_trial / evolution_promoted / evolution_rolled_back`（试验回写，约 L201-282）。

场景哈希 `_scenario_hash`：对 `scenario_id/scene_type/weather/traffic_profile + 订单压力(round 1位) + 意愿(round 1位)` 做 sha1 取前 16 位——同一类场景稳定映射到同一个键。

### 1.4 Read：记忆如何召回（检索增强决策）

入口：`recall_similar_context(features, candidates, limit=5, mode)`（约 L56-116）。`mode=="off"` 直接返回空包。

**第一步：加权相似度打分**（`_feature_similarity`，约 L566-578）——这是全系统最核心的一张权重表：

| 维度 | 权重 | 匹配方式 |
|---|---|---|
| scenario_id 场景ID | **0.22** | 精确相等 |
| scene_type 时段类型 | **0.18** | 精确相等 |
| weather 天气 | **0.14** | 精确相等 |
| order_pressure 订单压力 | **0.14** | 数值：`w × max(0, 1-|差|)` |
| courier_pressure 运力压力 | **0.10** | 数值 |
| avg_willingness 接单意愿 | **0.08** | 数值 |
| traffic_profile 交通画像 | **0.08** | 精确相等 |
| congestion_level 拥堵水平 | **0.06** | 数值 |

合计 1.00；最终得分归一化，`similarity ≤ 0` 的经验直接过滤。**注意这是部分匹配**：场景不必完全相同，越像分越高——前端"相似迁移"状态与此同构（见 §3.2）。

**第二步：四路召回，各取 Top-5**，打包成 `MemoryRecallBundle`：

| 通道 | 来源 | 排序键 |
|---|---|---|
| `scenario_memory` 场景记忆 | 事件流中 `scenario_seen/compare_completed` | (-相似度, event_id) |
| `decision_memory` 决策记忆 | 事件流中 `strategy_selected/rejected/compare_completed` | (-相似度, event_id) |
| `strategy_memory` 策略记忆 | 注册表（非进化条目），相似度取 `seen_contexts` 内最大 | (-相似度, -avg_score, id) |
| `evolution_memory` 进化记忆 | 注册表（进化条目） | 同上 |

### 1.5 记忆如何影响决策（Read 的落点）

`_fallback_rankings`（约 L404-435）把召回结果直接换算成排名加成：

```
memory_boost(算法) = similarity × ( avg_score × 0.22 + 状态加成 )
状态加成：promoted 7.0 ｜ trusted 4.0 ｜ candidate 2.0 ｜ draft 0.5
最终得分 = 当前 tick 实测 score + memory_boost   →  重排 → 排名第一者胜出
```

`autosolver_agent` 按 predictor 排名第一的候选选择底层算法（`compare_engine.py` 约 L436-473）。也就是说：**记忆不是展示品，它通过 memory_boost 真实改变了"这一轮用哪个算法"**。被 `promoted` 的策略在相似场景下有近乎压倒性的先验优势（+7.0 起步），这就是"进入 Planner 前的全局先验"。

### 1.6 Reflection / 回写：置信度状态机

每次对比后注册表状态按规则演进（约 L453-467）：

```
selected_count ≥ 2 且 avg_score ≥ 68        → promoted   （晋升为全局先验）
attempts ≥ 2 且 selected_count > 0          → trusted    （可信）
attempts ≥ 3 且 selected_count = 0 且 avg_score < 45
                                            → rolled_back（移出活跃池，rollback_action 记录）
其余                                         → candidate  （候选观察期）
```

进化候选另有独立判定 `_evolution_status`（约 L597-604）：`accepted 且 selected_count≥2 且 score≥68 → promoted`。

这就是 **Reflexion 式"按真实结果强化或抑制"**：赢得多→晋升→下次先验更强；连败→回滚→不再被召回抬分。回滚机制同时充当"经验淘汰"（防止坏经验污染检索）。

### 1.7 memory_mode 三档与 API 面

`memory_mode ∈ { off, read-only, read-write }`：off 不读不写；read-only 只召回不沉淀；read-write 完整闭环。

对外接口（`server.py`）：

| API | 作用 |
|---|---|
| `POST /api/compare/run`（可带 memory_mode / predictor_mode） | 跑一次对比，响应含 `memory`（RecallBundle 全量）与 `predictor.ranked_algorithms[].memory_boost` |
| `GET /api/memory/recall`（11 个场景特征做 query） | 只读召回 |
| `POST /api/memory/event` | 追加一条记忆事件（append-only） |
| `POST /api/predictor/rank` | 对既有 compare_run 重排 |
| `POST /api/day-simulation/run` / `GET /api/day-simulation/memory` | 全天推演及其记忆事件（支持按 frame_id 过滤） |

---

## 2. 推演叙事层（全天回放中的记忆事件）

**文件**：`/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/day_simulation.py`

全天推演（07:00→23:00，40 个决策帧）为每一帧生成**三连记忆事件** `EvolutionMemoryEvent`（`_build_evolution_memory_events`，约 L1020-1075）：

| 事件 | event_type | 语义 | 置信度变化 |
|---|---|---|---|
| 召回 | `memory_recall` | 评分前召回历史经验 | before = after（不变） |
| 回写 | `memory_writeback` | 本轮结果写回 | before → after（上升） |
| 策略整理 | `future_policy_shift` | 提炼一条未来策略 | after 再 +0.045（封顶 0.98） |

**关键字段**：
- `context_signature` 场景签名：`时段|天气|拥堵档|运力档|冲击列表`（`_context_signature`，约 L1125-1129），如 `lunch_peak|rain|high_congestion|scarce_supply|S-rain-lunch,S-merchant-burst-lunch`。拥堵分档：≥0.68 高 / ≥0.45 中 / 其余低；运力：≤18 人为 scarce。
- `recalled_case_ids` 召回案例：形如 `steady-lunch_peak-389`（平稳·午高峰类案例）、`similar-clear-62`（相似·晴天类案例），由种子稳定生成，代表跨天记忆库中的历史案例。
- `learned_rule` / 策略文本：由本帧真实收益决定措辞（正收益→"优先风险均衡派单"；弱收益→"保留贪心作护栏"）。

**置信度公式**（与真实收益挂钩，约 L1140-1151）：

```
confidence_before = clamp( 0.52 + 0.035×冲击数 + min(0.08, 单骑比×0.06)
                           + 0.04·(时间成本双正收益), ≤0.9 )
confidence_after  = clamp( before + 0.055
                           + min(0.16, 节省秒/180000 + 节省元/6000)
                           + 0.035·(超时风险下降), ≤0.96 )
```

即：**这一轮省得越多，回写后的置信度涨得越多**——学习曲线下方那条置信度细带的每一步都由此而来。

**诚实边界（评委追问必备）**：`memory_mode=off` 在日推演里只是不生成这些叙事事件（L1024-1025），**不改变**派单结果——因为日推演是"基线 vs 我方"同天双跑的回放。因此"带记忆 vs 不带记忆"的消融对比在这一层做不了；真正改变排名的记忆在 §1 的 compare 路径。前端因此改用"首遇/迁移/复遇的轮次归因"来证明记忆有效（见 §3），全部数字可逐轮复现。

---

## 3. 可视化层（「长期记忆」页如何证明记忆有用）

**文件**：`/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/day_replay_frontend_v2.py`（单文件 SPA，记忆页含 `renderMemoryPage` + `hydrateMemoryPage/teardownMemoryPage` 及一组 `memory*` 派生函数）

**总原则：不预置任何结论。**页面所有数字在浏览器端由 `workbench.decisions`（40 轮，`round_result.time_saved_min` 为累计值，逐轮差分得单轮节省）×`workbench.memory.items`（120 条 = 40 轮 × 召回/回写/策略）实时联表算出，评委可用页内数据表逐轮核对。

### 3.1 每轮的记忆状态：三态 + 可借鉴经验池

**签名相似度（前端）**：把 §1.4 的召回权重按签名五维**归并**（口径同源、可对照）：

```
时段 0.18 ｜ 天气 0.14 ｜ 拥堵 0.14 (=拥堵水平0.06+交通画像0.08)
运力 0.18 (=运力压力0.10+接单意愿0.08) ｜ 冲击 0.14 (=订单压力)
五维全同再加“同景加成”0.22（= scenario_id 权重），合计 1.0
```

**三态判定**（`memoryLearningRounds`）：

| 状态 | 判定 | 当天出现 |
|---|---|---|
| **全新场景**（橙色空心点） | 与所有已见场景相似度 < 0.5 | 仅 3 轮：10:12 开局冷启动、11:57 首遇暴雨、21:10 夜间缺人——恰好对齐三大剧情锚点 |
| **相似迁移**（蓝色点） | 签名首现但存在相似度 ≥ 0.5 的场景，借用其经验开局 | 5 轮，如 17:12 晚高峰·中拥堵 ←（相似 0.60）午高峰·中拥堵 |
| **同景复遇**（青绿实心点） | 签名完全命中 | 32 轮 |

**可借鉴经验池**（防"第 N 次复遇"读数跳水）：每轮显示 `此前相似度 ≥ 0.5 的历史轮数（同景 x + 相似 y）`。例：19:12 第 6 次复遇可借鉴 8 轮，19:27 虽是"本景第 1 次复遇"但可借鉴 **19 轮**——经验池与召回机制同构（经验池=粗筛候选，注入 Top-2=按相似度精排，即 Rerank 的"先海选后面试"）。"本景第 N 次 · 首现 HH:MM"仅作灰色小字溯源，不再当主信号。

### 3.2 页面六大模块

| 模块 | 内容 | 证明什么 |
|---|---|---|
| **① 证据条**（4 个大数字） | 全天累计节省 433.0 分钟（记忆参与的 37 轮贡献 417.2 分钟 / 96%）；记忆参与增益 ×2.1（全新均省 5.3 → 记忆参与均省 11.3 分钟/轮）；置信度 0.58 → 0.92；经验库 8 类场景 · 120 条事件 · 全新 3 / 迁移 5 / 复遇 32 | 记忆有效的总账 |
| **② 全天学习曲线**（上下双面板共享 X 轴，规避双轴图） | 上：累计节省面积图 + 40 个三态圆点 + 冲击时段底纹 + 高潮标注（"降雨+爆单 第 2 次复遇 单轮 +123.1 分钟"）；下：置信度细带；带 13 秒回放动画（clipPath 揭示 + 游标 + 数字实时增长）、十字线 tooltip、40 轮数据表兜底 | 学习过程随时间可见 |
| **③ 场景经验库矩阵** | 每行一类签名（8 行），空心橙=全新开局、空心蓝=迁移开局（标注来源）、实心=同景复遇，点大小∝该轮节省，弧线=首遇→复遇跨时段复用；行尾复用贡献与置信峰值 | 经验的沉淀与跨时段复用 |
| **④ 单轮召回链路**（四段动画流水线） | ①场景编码（展示 §1.4 真实权重表）→ ②相似经验召回（可借鉴经验池 + Top-K 案例芯片 + 迁移来源芯片，均可点击回跳）→ ③决策执行（本轮动作数与较基线节省）→ ④Reflexion 回写（置信度 before→after 动画 + 提炼的全局策略） | 单轮闭环的机制细节 |
| **⑤ 记忆分层 · Reflection 提炼**（漏斗） | 情景记忆 120 条（Write 逐轮写入）→ 语义记忆 8 类场景画像 + 3 类骑手/商圈/订单画像（Reflection 归纳）→ 策略记忆 4 条全局先验（Reflection 提炼，标注命中轮数与置信峰值）；"Read 一条顶多条" | 记忆不是流水账 |
| **⑥ 跨模块双向索引** | 曲线点↔数据表行互相定位高亮；召回案例芯片/迁移芯片→场景经验行滚动闪烁 | 评委"点哪亮哪"，四模块串成一条证据链 |

### 3.3 可视化设计红线（本版迭代中沉淀的原则）

1. **展示与机制同构**：召回是连续相似度，就不能用"精确匹配二值"展示（否则同页自相矛盾）→ 三态 + 相似度阈值。
2. **分组键的每个维度都必须可见**：签名含冲击维度，标题就必须带冲击（否则"同名不同计数"像标错）。
3. **按分组重置的计数不能当主信号**：会被读成"经验清零"→ 主指标用单调的经验池，次数降级小字。
4. **状态 UI 必须在默认路径可达**：迁移芯片只在开局轮出现 → 补"开局迁移"组级芯片。
5. 图表规范：不用双轴；系列色（青绿 #0d9488 / 琥珀 #b7791f / 蓝 #2563eb）过了 CVD 色盲校验；每张图配数据表兜底；`prefers-reduced-motion` 下关动画。

---

## 4. 术语映射（页面用语 ↔ Agent 概念 ↔ 代码落点）

| 页面用语 | Agent/RAG 概念 | 代码落点 |
|---|---|---|
| 评分前召回 / 记忆注入时机 | Read · 检索增强决策（给 Agent 装 RAG） | `recall_similar_context` → `_fallback_rankings` |
| 结果回写 | Write · Reflexion 反思回写 | `record_compare_result`、`_confidence_after` |
| 场景画像 / 全局策略提炼 | Reflection（复盘归纳，一条顶多条） | 注册表状态机、`future_policy_shift` |
| 场景编码 · 元数据过滤 | Self-Query / 先筛后搜 | `_scenario_hash` + `_feature_similarity` |
| 经验池粗筛 → Top-K 精选注入 | 粗排/精排（Rerank"先海选后面试"） | 四路召回 `limit=5`、前端经验池 |
| 情景/语义/策略记忆 | 情景记忆/语义记忆/隐性(过程性)记忆 | 事件流 / 画像 / 注册表 promoted 策略 |
| 决策执行 Generator-Critic | 生成-评审循环 | 候选算法评分 vs 基线对比 |
| candidate→trusted→promoted / rolled_back | 经验强化 / 淘汰（防污染） | `_updated_strategy_entry` |

> 术语红线：讲解只用上表词汇（均在团队学习笔记中出现过）；**避免** ACE、consolidation/记忆固化、decay/遗忘、procedural memory 英文原词等笔记中没有的说法。

---

## 5. 讲解口径与防追问 FAQ

**主线一句话**：*"系统一天只有 3 次真正从零开始（开局、暴雨、夜间缺人），其余 37 轮全部有记忆参与——要么同景复遇、要么相似迁移——这 37 轮贡献了全天 433 分钟节省的 96%。"*

**8 秒高潮**：*"11:57 第一次遇到暴雨只省 4.8 分钟；经验写回后，12:43 同类场景第 2 次复遇，单轮直接省 123.1 分钟。"*

| 可能的追问 | 回答口径 |
|---|---|
| 为什么不做"关掉记忆跑一遍"的消融？ | 日推演是同天双跑回放，关记忆只会不产生记忆事件、不改变派单；所以我们用更可复现的"全新/迁移/复遇轮次归因"，数据表逐轮可查。真正影响排名的记忆在 compare 路径，可用 `memory_mode=off/read-write` 分别调 `/api/compare/run` 对比 memory_boost。 |
| 相似度权重哪来的？ | `memory_engine._feature_similarity` 真实代码权重（0.22/0.18/0.14/0.14/0.10/0.08/0.08/0.06）；前端签名相似度是它按五维的归并，口径同源。 |
| 置信度是拍的吗？ | 公式挂钩真实收益：`after = before + 0.055 + min(0.16, 省时/180000 + 省钱/6000) + 0.035·风险下降`，省得多涨得多。 |
| "第 1 次复遇"是不是经验清零？ | 不是。它仍可借鉴全部相似场景积累（页面直接显示"可借鉴 N 轮（同景 x + 相似 y）"），召回从经验池粗筛后精选 Top-K 注入。 |
| 案例编号 #404 是什么？ | 跨天记忆库中的历史案例 ID（前缀=类型：平稳/冲击/相似），全新场景轮召回的就是这类跨天案例。 |
| 记忆多了会不会乱？ | 三重防线：相似度 ≤ 0 直接过滤；四路各取 Top-5；连败策略 rolled_back 移出活跃池。 |

---

## 6. 关键数字速查

| 指标 | 值 |
|---|---|
| 全天决策轮 / 记忆事件 | 40 轮 / 120 条（每轮召回+回写+策略） |
| 场景签名类数 | 8 类 |
| 三态分布 | 全新 3 · 迁移 5 · 复遇 32 |
| 全天累计节省 | 433.0 分钟（另省 ¥424.7、少 3 单超时） |
| 记忆参与轮贡献 | 417.2 分钟（96%） |
| 记忆参与增益 | ×2.1（5.3 → 11.3 分钟/轮） |
| 置信度轨迹 | 0.58 → 0.92（峰值） |
| 迁移阈值 / 同景加成 | 0.5 / 0.22 |
| 状态加成 | promoted 7.0 / trusted 4.0 / candidate 2.0 / draft 0.5 |
| 晋升/回滚阈值 | 选中≥2 且均分≥68 晋升；尝试≥3 未选中且均分<45 回滚 |

## 7. 文件索引

| 文件（绝对路径） | 角色 |
|---|---|
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/memory_engine.py` | 记忆引擎：存储/召回/加成/状态机 |
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/compare_engine.py` | 记忆的读写触发点与场景特征计算 |
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/day_simulation.py` | 推演叙事层：EvolutionMemoryEvent、签名、置信度公式 |
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/dispatch_workbench_data.py` | 把推演结果组装成前端 workbench.memory payload |
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/day_replay_frontend_v2.py` | 「长期记忆」页全部可视化（`renderMemoryPage` / `memoryLearningRounds` / `hydrateMemoryPage` 等） |
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/web_agent_demo/server.py` + `server_v2.py` | 记忆相关 API 与页面服务（`python3 web_agent_demo/server_v2.py --port 8793`） |
| `/Users/比赛/黑客松决赛-正式版/log-v4-claude-显示对比/_前端v2验证截图/` | 记忆页各状态验证截图（终态/回放中/联动/经验池等） |

> 注：文中引擎侧行号为"约"值，函数名为准；前端以函数名检索（该文件本轮迭代频繁，行号会漂移）。
